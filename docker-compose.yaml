version: ‘3’

services:
  redis:
    image: redis
    tty: True
    ports:
      - '6379:6379'
    volumes:
      - redis-data:/data

  web:
    build: .
    tty: True
    ports:
      - '8000:80'
    depends_on:
      - redis

volumes:
  redis-data:
